---
dependencies:
  - role: monit
  - role: java
  - role: qbroker
  - role: tomcat
  - { role: postgresql, when: web_frontend == 'nginx' }
  - { role: mysql, when: web_frontend == 'apache' }
  - { role: nginx, when: web_frontend == 'nginx' }
  - { role: apache2, when: web_frontend == 'apache' }
